% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdmvm_dag.R
\name{hdmvm_dag}
\alias{hdmvm_dag}
\title{Generate a tikz-cd DAG}
\usage{
hdmvm_dag(mod_boot_summ, p, q, trt_name = "trt", alpha = 0.1)
}
\arguments{
\item{mod_boot_summ}{A data frame; the output from \code{\link[=hdmvm_table]{hdmvm_table()}} with \code{DT_table = FALSE}.}

\item{p}{An integer denoting the number of candidate mediators i.e., \code{ncol(mediators)}.}

\item{q}{An integer denoting the number of outcomes i.e., \code{ncol(outcomes)}.}

\item{trt_name}{A character vector denoting the name of the treatment/exposure. By default, "trt" is used as a placeholder.}

\item{alpha}{A numeric between 0 and 1 denoting the nominal significance threshold for inclusion in the DAG. By default, 0.1 is used.}
}
\value{
A text string that, when passed through \code{cat()}, returns the \code{tikz-cd} code for the selected DAG.
}
\description{
\code{hdmvm_dag} generates a DAG using the syntax of the \code{tikz-cd} LaTeX package.
}
\examples{
\dontrun{
## Load toy data
data(hdmvmed_test_data)

## Assign mediator/confounder/trt/outcome matrices
mediators <- hdmvmed_test_data[,7:106]
confounders <- hdmvmed_test_data[,107:111]
trt <- hdmvmed_test_data[,1]
outcomes <- hdmvmed_test_data[,2:6]

## Fit the model
model_fit <- bootstrap_model(mediators = mediators, confounders = confounders,
                             trt = trt, outcomes = outcomes, nB = 10)

## Generate summary table
model_df <- hdmvm_table(model_fit, p = ncol(mediators),
                        outcomes = colnames(outcomes), DT_table = FALSE)

## Generate tikz-cd code
cat(hdmvm_dag(model_df, p = ncol(mediators), q = ncol(outcomes)))
}

}
