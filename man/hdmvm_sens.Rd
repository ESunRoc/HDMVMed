% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{hdmvm_sens}
\alias{hdmvm_sens}
\title{Sensitivity analysis for unmeasured mediator-outcome confounding}
\usage{
hdmvm_sens(
  fit,
  mediators = NULL,
  trt = NULL,
  ci_type = c("bca", "percentile"),
  level = 0.95,
  Q_method = c("both", "plugin", "crossfit"),
  signal_const = 1,
  sel_quantile = 0.1,
  report_only = TRUE,
  crossfit_folds = 5,
  seed = 823543
)
}
\arguments{
\item{fit}{The list returned by \code{\link[=bootstrap_model]{bootstrap_model()}}, with elements \code{res} and
\code{internals}.}

\item{mediators, trt}{Optional overrides for the mediator matrix and exposure vector.
\code{\link[=bootstrap_model]{bootstrap_model()}} retains both in \code{internals}, so these are normally left \code{NULL}
and taken from the fit; supply them only to compute \eqn{Q} on data other than that
used for the fit.}

\item{ci_type}{Which reported interval to use, \code{"bca"} (default) or \code{"percentile"}.}

\item{level}{Confidence level of the reported intervals; defaults to 0.95.}

\item{Q_method}{Method for estimating the design scalar \eqn{Q = \alpha\Sigma_M^{-1}\alpha^\top}.
Options are \code{"plugin"}, \code{"crossfit"}, or \code{"both"}; defaults to \code{"both"}. See details.}

\item{signal_const}{A minimum-signal threshold  \eqn{\theta_n = c\sqrt{s\log(pq)/n}};
defaults to 1. This should be varied as part of any report; see \code{\link[=hdmvm_sens_signal_sensitivity]{hdmvm_sens_signal_sensitivity()}}.}

\item{report_only}{A Boolean. If \code{TRUE} (the default), the erosion robustness values are
computed over \emph{reported findings} -- pairs that are both selected and whose interval
excludes zero -- rather than over every selected pair. Restricting in this way matters
whenever the penalty is loose: a selected set dominated by pairs with
\eqn{|\hat\phi^d| < \theta_n} drives both the minimum and any low quantile to zero, so
the unrestricted statistic reports the fragility of coefficients no analyst would
present. The unrestricted values are returned alongside as \code{RV_erode_all}.}

\item{crossfit_folds}{Number of folds for the cross-fitted \eqn{R^2_{A\sim M}}.}

\item{seed}{Seed for the cross-fitting split.}
}
\value{
An object of class \code{hdmvm_sens} with components:
\itemize{
\item \code{tide}, \code{de} -- data frames of robustness values per outcome;
\item \code{pide} -- data frame of robustness values per mediator-outcome pair;
\item \code{selection} -- the erosion and spurious-selection robustness values;
\item \code{benchmark} -- sensitivity parameters implied by each measured confounder;
\item \code{Q}, \code{R2_AM} -- the design scalar and the exposure-prediction \eqn{R^2};
\item \code{meta} -- dimensions, thresholds, and settings.
}
}
\description{
\code{hdmvm_sens()} performs a full sensitivity analysis for a fit \code{\link[=bootstrap_model]{bootstrap_model()}}.
It returns robustness values for every reported estimand, the selection robustness
values, and the benchmark values implied by the measured confounders.
}
\details{
\strong{The sensitivity parameters.} A hypothesized unmeasured confounder \eqn{Z},
characterized by two bounded numbers: \eqn{R^2_M}, the population \eqn{R^2} from
regressing \eqn{Z} on the mediator residuals, shared across outcomes; and
\eqn{R^2_Y(k)}, the fraction of outcome-\eqn{k} residual variance attributable to
\eqn{Z}. When \eqn{p=q=1} these collapse to the residual correlation of Imai and
Yamamoto (2013) by \eqn{\rho = \pm\sqrt{R^2_MR^2_Y}}.

\strong{Robustness values.} Reported on the symmetric scale \eqn{R^2_M = R^2_Y = RV}.
For an estimand with reported interval \eqn{[L,U]} and worst-case bias
\eqn{\kappa\sqrt{R^2_MR^2_Y}}, the robustness value is
\eqn{\min\{\min(|L|,|U|)/\kappa,\,1\}}. A value of 1 implies that no
confounder of the assumed form can overturn the finding. For individual coefficients,
this reduces to \eqn{(|t|-z)/\sqrt n}, free of any matrix quantity.

\strong{The design scalar.} \eqn{Q=R^2_{A\sim M}/[\sigma_A^2(1-R^2_{A\sim M})]} governs the
worst-case bias in every bound. A design in which the mediators nearly determine
the exposure is intrinsically fragile. The plug-in estimator \eqn{\hat\alpha\hat\Theta_M\hat\alpha^\top}
is upward biased (conservative) because \eqn{\hat\alpha} is dense; the cross-fitted estimator is
downward biased (anti-conservative) through regularization shrinkage. \code{Q_method = "both"}, the default,
takes the larger. The cross-fitted estimator is available without further input, at the cost of one cross-validated
lasso fit per fold. Use \code{Q_method = "plugin"} where that cost matters.

Moderated mediation is not (presently) supported: the bias formulas are derived for the
unmoderated model.
}
\examples{
\dontrun{
data(hdmvmed_test_data)
fit <- bootstrap_model(mediators = hdmvmed_test_data[, 7:106],
                       confounders = hdmvmed_test_data[, 107:111],
                       trt = hdmvmed_test_data[, 1],
                       outcomes = hdmvmed_test_data[, 2:6], nB = 500)
s <- hdmvm_sens(fit) # mediators/trt taken from fit$internals
s                    # print method: headline table
plot(s, outcome = 1) # contour plot in the (R2_M, R2_Y) plane
}

}
\references{
{
Cinelli, C. and Hazlett, C. (2020). Making Sense of Sensitivity: Extending Omitted
Variable Bias. \emph{JRSS-B}, \bold{82}(1), 39-67.
}

{
Imai, K., Keele, L., and Yamamoto, T. (2010). Identification, Inference and
Sensitivity Analysis for Causal Mediation Effects. \emph{Statistical Science},
\bold{25}(1), 51-71.
}

{
Sun, E., Xiao, J., and Wu, T. T. (2026). Causal Mediation Analysis for Multiple
Outcomes and High-dimensional Mediators. \emph{Biostatistics}, under review.
}
}
\seealso{
\code{\link[=hdmvm_sens_bias]{hdmvm_sens_bias()}}, \code{\link[=hdmvm_sens_signal_sensitivity]{hdmvm_sens_signal_sensitivity()}}
}
