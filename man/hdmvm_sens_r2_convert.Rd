% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_analysis.R
\name{hdmvm_sens_r2_convert}
\alias{hdmvm_sens_r2_convert}
\title{Convert between marginal and partial outcome-side sensitivity parameters}
\usage{
hdmvm_sens_r2_convert(R2, R2_M, to = c("partial", "marginal"))
}
\arguments{
\item{R2}{Numeric in \eqn{[0,1]}: the value to convert.}

\item{R2_M}{Numeric in \eqn{[0,1)}: the mediator-side sensitivity parameter.}

\item{to}{Either \code{"partial"} (the default; treats \code{R2} as marginal) or \code{"marginal"}
(treats \code{R2} as partial).}
}
\value{
A numeric of the same length as \code{R2}.
}
\description{
The default outcome-side parameter, \eqn{R^2_Y(k)}, is the \emph{marginal}
association of the confounder with the outcome residual (in keeping with Imai,
Keele, and Yamamoto (2010)). Cinelli and Hazlett (2020) instead use the \emph{partial}
association given the included regressors, which here means conditioning on the
mediators. The two differ whenever \eqn{R^2_M>0}, since the confounder is correlated
with the mediators. \code{hdmvm_sens_r2_convert()} provides a map between the two.
}
\details{
The relationship is
\deqn{R^{2,\mathrm{par}}_Y(k) = \frac{R^2_Y(k)\,(1-R^2_M)}{1-R^2_Y(k)\,R^2_M},}
which is monotone in \eqn{R^2_Y(k)} and satisfies
\eqn{R^{2,\mathrm{par}}_Y(k)\le R^2_Y(k)}, with equality iff
\eqn{R^2_M = 0}. Either parameterization may be elicited provided the choice is stated.
}
\examples{
hdmvm_sens_r2_convert(0.20, R2_M = 0.30)                  # marginal -> partial
hdmvm_sens_r2_convert(0.20, R2_M = 0.30, to = "marginal") # and back

}
\seealso{
\code{\link[=hdmvm_sens]{hdmvm_sens()}}
}
